{{/*
ServiceAccount used only by components that opt into leader election (e.g.
orion-ld active/standby HA). The leader-elector sidecar needs RBAC on Leases;
see rbac.yaml.
*/}}
{{- range $name, $component := .Values.components }}
{{- if and $component.enabled $component.leaderElection }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "pid-gijon-core.kebabcase" $name }}
  labels:
    {{- include "pid-gijon-core.labels" $ | nindent 4 }}
    service: pid-gijon-{{ include "pid-gijon-core.kebabcase" $name }}
{{- end }}
{{- end }}
