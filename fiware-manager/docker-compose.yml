services:
  fiware-manager:
    container_name: fiware-manager
    build:
      context: .
      target: dev
    hostname: fiware-manager
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - '.:/code'
    ports:
      - 30006:8000
