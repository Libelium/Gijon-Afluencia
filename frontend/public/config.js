/*
 * Configuracion de ejecucion. En desarrollo va vacia a proposito: manda el fichero .env.
 * En el contenedor, deploy/entrypoint.sh sobrescribe este fichero con las variables de
 * entorno en cada arranque. Ver docs/deployment.md.
 */
window.__APP_CONFIG__ = Object.freeze({});
